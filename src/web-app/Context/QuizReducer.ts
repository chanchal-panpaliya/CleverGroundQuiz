const QuizState:any = { quizlist: [] };
  
const QuizReducer=(state: typeof QuizState, action: any)=>{
    switch(action.type){  
        default :
        return state
    }
}

export default QuizReducer;